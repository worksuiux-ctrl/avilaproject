# Avilaproject

## Repositorios

| Rol | URL |
|-----|-----|
| **Principal** (trabajo diario) | `http://200.74.216.124:8906/dise-o_ux_ui/avilaproject.git` |
| **Respaldo** (copia estática) | `https://github.com/worksuiux-ctrl/avilaproject.git` |

> ⚠️ El repo de **GitHub es solo respaldo**. Todo el desarrollo, ramas, merge requests y despliegues se hacen desde **GitLab**.

## Prototipo funcional

| URL | Propósito |
|-----|-----------|
| `https://coe-evolution.site/prototipotesting2026/` | Build de `dist/` para pruebas del equipo funcional |
| `https://github.com/worksuiux-ctrl/teaser_coevolution` | Repo donde se aloja el build |

> El sitio **coe-evolution.site** contiene únicamente el `dist/` generado con `npm run build`. No es el código fuente. Se actualiza manualmente copiando la carpeta `dist/` del proyecto al repo `teaser_coevolution`.

